import CreateExperimentTile from "./CreateExperimentTile";

const Home = () => {
  return (
    <div className="flex h-full w-full grow flex-col items-center justify-center">
      <CreateExperimentTile />
    </div>
  );
};

export default Home;
